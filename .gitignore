# Binaries
bin/
dist/
models/

# ORT working directory (created by scripts/download-ort.sh before GoReleaser runs)
ort-lib/

# Tokenizers static lib (created by scripts/download-tokenizers.sh before GoReleaser runs)
tokenizers-lib/

# Build artifacts
*.exe
*.dll
*.so
*.dylib

# Brainfiles (local memory databases — never commit)
*.fbrain

# Test artifacts
*.test
*.out
reports/

# Go workspace
vendor/
# UI embedded JS vendor (not a Go vendor dir)
!internal/ui/assets/vendor/

# Models cache (downloaded at runtime)
.cache/
*.onnx
*.ort

# Environment / secrets
.env
*.env
!dist/firequery-models.env

# Editor
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db

# Python (optional backend)
__pycache__/
*.pyc
*.pyo
.venv-firequery/
docs/TASKS.md

# Development task lists (local only)
